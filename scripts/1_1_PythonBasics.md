# >> 2 + 2   // is an expression

# order of operations
# + - * / // **

#Data types: int, float, str
#strings use single quotes

#storing data in variables
x = 2
#variable name conventions

print('Hello World')
print('What is your name?')
name = input('>')
print('It is good to meet you, ' + name)
print('The length of your name is:')
print(len(name))
print('What is your age?')
age = input('>')
print('You will be ' + str(int(age) + 1) + ' years old in a year.')

#print('') function python's Console.Writeline(" ")
#input() python's Console.Readline();

print('I am '+ str(29) + ' years old in a year.')

#Basic data type conversion
# The str(), int(), and float() functions will evaluate to the string, integer, and floating-point forms of the value you pass, respectively.

#type() function
x = 10
print(type(x))

#round() abs()
#round off number and returns absolute value respectively

